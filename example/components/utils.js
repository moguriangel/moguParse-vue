
const constNotDocumented = {
  utils: 'some vars on js file'
};

/**
 * @description desc of const arrayUtils
 */
const arrayUtils = [1, 2, 3]

/**
 * @param {String|Number} text some text
 * @description desc functionUtils
 * @returns {String} return a concatenated string
 */
const functionUtils = (text) => {
  return 'awesome func' + text
}